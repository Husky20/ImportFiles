{% extends 'base.html.twig' %}

{% block body %}
    <div class="success-container">
        <h1>Add CSV file</h1>
        <div class="form">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn">Add</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
